package parser

// ParseHTML analyzes HTML content and extracts data based on config or rules.
// We'll expand this to handle specific selectors, attributes, etc.
func ParseHTML(htmlContent string) (map[string]string, error) {
	// Stub: for now, just return an empty map
	return map[string]string{}, nil
}
